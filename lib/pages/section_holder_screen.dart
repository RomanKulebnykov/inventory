import 'package:flutter/material.dart';

class SectionHolderScreen extends StatelessWidget {
  const SectionHolderScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
