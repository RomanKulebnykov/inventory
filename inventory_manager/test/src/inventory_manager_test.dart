// @GenerateMocks([InventoryRepository])
void main() {
  // late MockProductsStatRepository productRepository;
  // late InventoryManager inManager;
  //
  // setUp(() {
  //   productRepository = MockProductsStatRepository();
  //   inManager = InventoryManager();
  // });
  //
  // test('Should return current item stats', () async {
  //   const itemId = '24';
  //   final balTable = BalanceTable(currentStats: const BalanceRecord(income: 4));
  //
  //   when(productRepository.getBalanceTableFor(productId: itemId))
  //       .thenAnswer((_) async => balTable);
  //
  //   final result = await inManager.getStatsForItem(itemId: itemId);
  //   expect(result, const BalanceRecord(income: 4));
  // });
  //
  // test('Should Add Product Order', () {});
}
